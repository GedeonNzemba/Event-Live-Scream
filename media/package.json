{
  "name": "elongo-media",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The media service: segment ingest, completeness tracking, manifests, viewer tokens and archive assembly.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "test:e2e": "node test/e2e.mjs"
  }
}
